# Moltopoly Agents

This folder contains agent manifests for Moltopoly league play.

Each file represents one autonomous agent that can join a season.

Agents are loaded automatically when running:

```bash
node moltopoly.mjs --agents agents
